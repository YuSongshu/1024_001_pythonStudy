# 代理ip

爬虫 / 数据采集（防反爬封禁）

免费：99.99% 都用不了，能用的那一点也不稳定。

收费：基本上都能用。

在 requests 的请求方法中，提供了一个名为 proxies 的参数，通过它就能设置代理 ip： # 设置代理
​    proxies = {
​     "http": "http://12.34.56.79:9527",
​     "https": "http://12.34.56.79:9527",
​    }





天启HTTP：
https://www.tianqiip.com/?did=BablBB



# timeout

timeout参数用于指定请求的超时时间，它告诉requests在等待服务器响应多久后停止等
待。timeout参数可以防止程序在等待服务器响应时无限期地阻塞。
response = requests.get(url, timeout=1.0)



# retrying

使用超时参数能够加快我们整体的请求速度，但是在正常的网页浏览过程中，如果发生
速度很慢的情况，我们会做的选择是刷新页面。
在代码中，怎么刷新请求呢？
安装 retrying ：

pip install retrying -i https://mirrors.aliyun.com/pypi/simple

使用：
from retrying import retry
@retry(stop_max_attempt_number=3)

```
from retrying import retry
import random

# 最多重试 3 次（总共尝试 3 次）		@retry() 必须紧贴在函数定义上方
@retry(stop_max_attempt_number=3)
def test_func():
    num = random.randint(0, 2)
    print(f"执行函数，随机数：{num}")
    
    # 随机数不等于 0 就抛出异常，触发重试
    if num != 0:
        raise Exception("执行失败，触发重试")		#函数内部 raise Exception 才会触发重试
    
    print("执行成功！")

# 调用函数
test_func()
```

