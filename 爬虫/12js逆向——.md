# 初识js逆向

在程序中，为了保护数据，通常会加密运输。加密操作通常是js代码实现，js逆向则是从加密的已知参数逆向找到它的位置

## 准备工作

安装 node.js

Node.js 是一个基于 Chrome V8 引擎的 JavaScript 运行时环境，提供了执行和解释 
JavaScript 代码的功能。

# JS基础语法

JavaScript是一种广泛使用的编程语言，常用于网页和网站的开发。
JavaScript的语法包括变量、数据类型、运算符、条件语句、循环语句、函数等基
本概念

基础语法参考：https://www.cnblogs.com/xtazm/articles/19096812





# 执行js代码

Python 如果要执行JS代码，可以安装 PyExecJS。
安装：pip install PyExecJS
使用：

1.  编译 js 文件
    execjs.compile()：编译 JavaScript 代码调用 js 中的方法
    参数：需要编译的 JavaScript 代码字符串
    返回值：JavaScript 运行的上下文
2. context.call()：执行 JavaScript 代码中的函数，并返回其执行结果
    参数：
    • js 函数名称
    • js 函数参数...
    返回值：函数运行的结果

```python
import execjs
# 1. 编写 JS 代码
js_code = """
function add(a, b) {
    return a + b;
}
function md5Sign(data, salt) {
    const crypto = require('crypto');
    return crypto.createHash('md5').update(data + salt).digest('hex');
}
"""

# 2. 编译 JS → 得到 Context（上下文）
ctx = execjs.compile(js_code)

# 3. 调用 JS 函数
res1 = ctx.call("add", 10, 20)          # 30
res2 = ctx.call("md5Sign", "hello", "123")  # MD5 结果
print(res1, res2)
```



**拓展（从文件读取 JS 代码）**

```python
import execjs
import time

# 从文件读取 JS 代码
with open("sign.js", "r", encoding="utf-8") as f:
    js_code = f.read()

ctx = execjs.compile(js_code)

ts = str(int(time.time()))
data = '{"id": 1001, "name": "test"}'
sign = ctx.call("getSign", ts, data)
print("timestamp:", ts)
print("sign:", sign)
```





# 异步请求

AJAX（Asynchronous JavaScript and XML）是一种在不重新加载整个网页的情况下，能够更新部分网页的技术。虽然名称中包含 XML，但现代的 AJAX 实现通常使用 JSON 或其他数据格式代替 XML。

## 原生 ajax

开始 ajax 请求的第一步创建一个 XMLHttpRequest 对象，这个对象是浏览器提供的 API，用于与服务器进行异步通信。

```js
function getData() {
    // 1、创建一个请求对象
    xhr = new XMLHttpRequest();
    // 2、通过open方法设置请求的url和请求的参数
    xhr.open('GET', 'http://...', true);
    // 3、设置ajax请求发送之后的回调函数
    xhr.onload = function () {
        console.log('响应状态码:', xhr.status);
        console.log('响应体内容', xhr.responseText);
    }
    // 4、请求头的设置
    xhr.setRequestHeader('token', '12345678');

    // 5、发送请求
    xhr.send();
}
```

open()：设置请求类型（GET、POST等）、URL和是否异步执行；

setRequestHeader()：设置请求头；

send()：发送请求，该函数的参数是请求的参数。



## jQuery ajax

‌jQuery AJAX‌ 是 jQuery 库提供的一组用于执行异步 HTTP 请求的方法，它封装了浏览器原生的 XMLHttpRequest 对象，简化了客户端与服务器之间的数据交互操作。

使用 jQuery ajax 时，需要先获取 jQuery 库，可以直接导入在线的 jQuery：

```js
function getData() {
    $.ajax({
        url: "http://...",
        type: "get",
        headers: {
            'token': '12345678'
        },
        success: function (response, textStatus, jqXHR) {
            console.log('响应状态码:', jqXHR.status);
            console.log('响应状态:', textStatus);
            console.log('响应体:', response);
        }
    });
}
```

url：表示目标请求的 url。

type：表示发送请求的方式；

headers：表示设置的请求头；

success：表示请求成功后回调的方法。

**注：**

回调：
​    回来调用
​    对于发送请求来说：
​        去：发送请求
​        来【回来】：接收响应
​            接收到响应数据之后要调用的函数。
​            回调函数：回来的时候，调用的函数。
​            干嘛？
​                回来的函数处理响应的数据【解密、页面渲染】。
jQuery 是一个前端的库，这个文件一般不会随便修改里面内容。





## Fetch API

Fetch API 提供了一种更简单、更强大的方式来进行 HTTP 请求，相比传统的 XMLHttpRequest 更加现代化和易于使用。

```js
async function getData() {
    let url = 'http://...'
    let config = {
        method: "GET",
        headers: {
            "token": "12345678"
        }
    };
    try {
        const response = await window.fetch(url, config)
        console.log('请求成功:', response)
    } catch (error) {
        console.log('请求发送出现错误了:', error)
    }
}
```







# 浏览器开发者工具：

学习前面的目的，是为了能看懂 js 代码。
浏览器开发者工具：
​    看网页的 js 代码。
​    debugger =》调试。
​        非常 非常 非常 重要。

打开控制台的方式：
​    第一种：点击右下角的向上箭头
​    第二种：
​        1. 点击开发者工具右上角的 ...
​        2. 在弹出的界面点击“搜索”
​        3. 在下面弹出的框框中点击 “控制台”



一定不要点暂停情况下的最左侧按钮【||】。
三角形的：
​    后面没有继续断。
转弯箭头：
​    单行执行
​    不进入函数
向下的箭头：
​    进入函数里面【如果有函数调用】
向上的箭头：
​    当前代码后面的部分不看了。

注意事项：
​    所有断下来的地方一定是程序执行到的地方。



## 调用堆栈

特点：先进后出，后进先出

作用：管理函数上下文，分析调试指南针



## XHR提取断点

### 核心用途

1. 抓动态加载、翻页、提交表单等**隐藏接口**（请求路径或请求路径的部分）

2. 调试请求参数、请求头、响应数据异常

3. 定位哪行代码发起了请求（看调用栈）

4. 拦截并修改请求 / 响应（配合调试器）



注：

**发起程序**也可以实现类似效果

