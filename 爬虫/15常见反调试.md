# 15常见反调试

网站为防止逆向人员打开 F12 调试、扒取加密算法、窃取签名逻辑，在 JS 内置**检测调试环境 + 干扰调试**的代码；一旦检测到开发者工具打开、代码被断点调试，就会触发：无限断点、页面跳转、清空数据、死循环、加密逻辑失效等对抗行为。

https://www.aqistudy.cn/

## test

```
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <script src="fts.js"></script>
    <style>
        body {
            margin: 0;
            height: 100vh;
            font-size: 50px;
            color: #03b7bf;
            display: flex;
            align-items: center;
            justify-content: center;
        }
    </style>
</head>
<body>
    hello 反调试
</body>
</html>
```

<script src="fts.js"></script> 

配置文件:   **fts.js**







## 阻止打开开发者工具

### 禁用鼠标右键

实现代码

```
// 监听鼠标右键事件，防止调试工具被使用
document.oncontextmenu = () => {
    alert('检测到非法调试，右键被管理员禁用');
    return false;
}
```

`document.oncontextmenu` 表示监听鼠标右键；

`return false` 表示禁止后续事件操作。

`alert`表示警告弹窗、提示框输出



### 禁用 F12

```
// 监听键盘按键事件，防止调试工具被使用
document.onkeydown = () => {
    // 键盘监听事件对象
    var e = window.event || arguments[0];
    // 检测按键是否是 F12
    if (e.keyCode == 123) {
        alert("检测到非法调试，F12被管理员禁用");
        // 阻止事件继续执行
        return false;
    }
}
```

`123` 在按键中表示 `F12`。



#### 解决方案

控制台打开的几种方式：

1. F12
2. ctrl+shif+i
3. 鼠标右键 -》检查
4. “三点” -》更多工具 -》开发者工具



注

并不是所有的检测到后都给提示，有的甚至会直接跳转到其他地方，或者做其他操作。例如：

```js
document.onkeydown = () => {
    var e = window.event || arguments[0];
    if (e.keyCode == 123) {
        // alert("检测到非法调试，F12被管理员禁用");
        // 跳转到百度
        window.location.href = "https://www.baidu.com";
        return false;
    }
}
```



### 无限debugger

debugger 是 js 的关键字，写在哪就可以在哪设置断点。

#### eval

`eval` 函数可以**将字符串当做代码执行**，也就是说你在字符串中写运算，最后得到的也是运算后的值，而不是加法运算的字符串。

```
eval("debugger");
```

会生成一个文件（VM开头，这种文件叫虚拟文件)



#### Function

会生成一个虚拟文件，并在一个函数中包含起来

```
Function("debugger")();
```

```
(function () {}["constructor"]("debugger")());
```

`function () {}` 是一个匿名函数

`["constructor"]` 调用函数的构造函数，这里指向的就是 Function 了

所以上面代码等价 `Function("debugger")()`；



#### 无限

1.循环

while Ture

2.计时器

```
// 每1000ms执行一次，永久循环
setInterval(function () {
    debugger;
}, 500);
// 停止计时器
// clearInterval(timer);
```

```
setInterval = function () {
    console.log("修改定时器");
    return function(){};
}							//全局覆盖，消去参数，以至于后面的不执行

setInterval(function () {
    debugger;
}, 5000);			//本质是调用
```

3.递归

```
import time 
n = 1
def f1():
    global n
    print('start')
    print(f'f:{n}')
    time.sleep(1)
    n += 1
    f2()

def f2():
    print('f2')
    time.sleep(1)
    f3()
   
def f3():
    print('f3')
    time.sleep(1)
    f1()

f1()
```



#### 解决方案

1.右键点击 debugger 处，点击永不在此处暂停

2.hook —— 针对 计时器与递归



### 窗口检测

窗口检测一般检测的是内外窗口的尺寸，这个时候一般是在控制台打开之后的。检测后的常见操作：

清空控制台

跳转页面

假数据替换



示例

```
// 设置定时器
setInterval(
    // 定时器执行的操作
    function () {
        // 检测浏览器外窗口尺寸和内窗口尺寸的差值   单位 像素
        if (window.outerWidth - window.innerWidth > 300 || window.outerHeight - window.innerHeight > 300) {
            // 清除控制台
            console.clear()
        }
    },
    // 定时执行的时间，每多久执行一次 ms
    500
);
```

这里主要用到了：

定时器：setInterval(定时任务函数，定时时间)

内外窗口尺寸检测

清除控制台：console.clear()

关闭网站：window.close()



#### 解决方案

控制台悬浮出来：点击控制台右侧三点，`停靠测` 部分选择第一个

注：可以提前悬浮控制台，再打开网站



### 格式化检测



```
// 格式化检测
var test = function (){return '666';}

// 设置定时器每秒检测一次函数是否被格式化
setInterval(function () {
    // 定义正则表达式来匹配格式化后的函数
    var reg = new RegExp("\\w+ \\(\\){\\w+ ['|\"].+['|\"];}");
    // 匹配到了，结果返回 true
    var isFormatted = reg.test(test.toString());

    if (!isFormatted) {
        console.log("函数被格式化");
    }
}, 1000);
```

实现格式化检测，主要依赖于两个：

1.`正则表达式`：匹配符合条件的格式，reg.test 表示检测是否有符合条件的字符串，有则返回 true

2.`toString() 方法`：打印函数的格式

格式化代码

```
var test = function (){return '666';}
test.toString() 
```

返回

```
"function (){return '666';}"
```



```
var test = function (){
	return '666';
}
test.toString() 
```

返回

```
"function (){\r\n\treturn '666';\r\n}"
```

两个返回不一样故可以通过定义正则表达式来匹配格式化后的函数



注:可以通过在线工具压缩

```
https://www.sojson.com/format/jsyasuo.html
```

代码替换 —— 替换的是浏览器读取的，并不能替换网站【服务器】：
1.点击开发者工具左侧箭头

2.点击覆盖

3.点击 + 号

4.创建文件夹，保存之后覆盖的代码

5.点击弹出的允许按钮

6.右键点击要被覆盖的 js 文件，点击替代内容，会将 js 文件保存在本地【右下角有紫色原点才是成功】





#### 内存爆破

实现内存爆破的方式也挺简单，即无限循环的做某件事情

```
var test = function () {
    return '666';
}

function dbf() {
    let memoryLeak = [];
    // 每隔10毫秒就向数组中添加一个大的对象
    setInterval(() => {
        let obj = {};
        // 给对象添加大量属性，以增加其占用的内存
        for (let i = 0; i < 500000; i++) {
            obj['key' + i] = Math.random();
        }
        memoryLeak.push(obj);

        // 可选：输出当前数组长度，以监控内存泄漏
        console.log(memoryLeak.length);
    }, 1);
}
```





### 混淆

混淆：增加阅读代码的难度

性能没有好处，这种代码不会很多。

#### 解决方案

1.ast 解混淆【要求 js 非常非常好】

2.在线工具

```
https://webfem.com/tools/js-decode/index.html
```

注：能不能用不好说

3.控制台打印

++
    自增
        前：先自增【先改变自己的值，然后改变的值参与运算】
        后：后自增【先参与运算，后改变自己的值，在下次运算的时候会改变】

流程平坦化【平坦流】（demo）

```
console.log(1);
console.log(2);
console.log(3);
console.log(4);
console.log(5);
console.log(6);
console.log(7);
console.log(8);
console.log(9);
console.log(10);

var arr = [7,3,6,9,4,5,2,8,1,10];
var a = 0;
while (true) {
    switch (a) {
        case 1:
            console.log(1);
            break;
        case 2:
            console.log(2);
            break;
        case 3:
            break;
        case 4:
            break;
        case 5:
            break;
        case 6:
            break;
        case 7:
            break;
        case 8:
            break;
        case 9:
            break;
        case 10:
            break;
    }
}
```





