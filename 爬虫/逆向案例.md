# 逆向案例

## 深交所

网址：[深交所投资者服务通行证](https://owssso.szse.cn/sso/login;jsessionid=A2D0B28065A173D92CB8CC0DFC5D45D5?service=https://www.szse.cn/application/passCenter/pass/&locale=zh) 

接口：https://owssso.szse.cn/sso/login;jsessionid=A2D0B28065A173D92CB8CC0DFC5D45D5?service=https://www.szse.cn/application/passCenter/pass/&locale=zh

需求：password =》MTIzNDU2

最终要实现的目的：让我们输入的 “123456” 能变成 “MTIzNDU2”。

明文 =》明文

### 逆向

1. 定位：

   搜索【全局搜索】关键字：【password】

   - 排除
   - 排除不了的打断点，执行
   - 分析执行到的地方的数据
   - 跟着代码调试，直到找到加密的位置

2. 实现

   扣代码【必须】：

   - 将找到的代码复制到本地，先补全参数，再补全函数【根据执行提示报错补全代码】

   - 函数：

     1）将函数`名称`粘贴到控制台打印，点击打印出来的内容；

     2）将鼠标放到函数名称位置，在弹出的框框中点击蓝色字体；

     3）向下的箭头进入函数；


## 网易云评论

网址：[爱的交响 - 张伦硕 - 单曲 - 网易云音乐](https://music.163.com/#/song?id=204072) 

需求：获取歌曲的评论

接口：https://music.163.com/weapi/comment/resource/comments/get?csrf_token=264449941cdd556f54939af1d085a1f2



### 定位请求

搜索歌曲关键字定位：https://music.163.com/weapi/comment/resource/comments/get?csrf_token=264449941cdd556f54939af1d085a1f2

分析翻页规律：

- 第二页：

  ![image-20251219203015315](D:/tool/%E7%88%AC%E8%99%AB2/2511%E6%9C%9F2%E7%8F%AD%E7%88%AC%E8%99%AB-harry%E8%80%81%E5%B8%88/13.%E6%89%A3%E5%8F%96js%E4%BB%A3%E7%A0%81/02.%E4%BB%A3%E7%A0%81/assets/image-20251219203015315.png)

- 第三页：

  ![image-20251219203027185](D:/tool/%E7%88%AC%E8%99%AB2/2511%E6%9C%9F2%E7%8F%AD%E7%88%AC%E8%99%AB-harry%E8%80%81%E5%B8%88/13.%E6%89%A3%E5%8F%96js%E4%BB%A3%E7%A0%81/02.%E4%BB%A3%E7%A0%81/assets/image-20251219203027185.png)

- 第四页：

  ![image-20251219203038573](D:/tool/%E7%88%AC%E8%99%AB2/2511%E6%9C%9F2%E7%8F%AD%E7%88%AC%E8%99%AB-harry%E8%80%81%E5%B8%88/13.%E6%89%A3%E5%8F%96js%E4%BB%A3%E7%A0%81/02.%E4%BB%A3%E7%A0%81/assets/image-20251219203038573.png)

没法分析规律【加密了 =》如何加密的】。

猜测加密之前的明文里面应该有包含与页数相关的内容。

### 逆向

#### 定位

1. 确定逆向数据

   ![image-20251219203436942](D:/tool/%E7%88%AC%E8%99%AB2/2511%E6%9C%9F2%E7%8F%AD%E7%88%AC%E8%99%AB-harry%E8%80%81%E5%B8%88/13.%E6%89%A3%E5%8F%96js%E4%BB%A3%E7%A0%81/02.%E4%BB%A3%E7%A0%81/assets/image-20251219203436942.png)

   上面的参数中：

   - csrf_token 是固定死的【多次请求都不会发生变化】

   - 如果一个参数可以随意设置，都可以得到结果【不需要逆向】

     控制变量法，每次测试后只能修改一个，其他的保持不变。

   确定 `params` 和 `encSecKey`。

2. 定位参数位置

   全局搜索：

   - 搜索的时候尽量搜不常见的关键字

   - 缩小范围

     ```js
     encSecKey: 
     encSecKey= 
     ```

     密文是值，要赋值给变量【属性，参数】，这个参数才能得到这个值。

   搜不到结果怎么办？【后面定位里面讲】

3. 搜索到的所有可疑的地方打断点，继续执行程序

   ```js
   var bVC0x = window.asrsea(JSON.stringify(i1x), bss6m(["流泪", "强"]), bss6m(Af6Z.md), bss6m(["爱心", "女孩", "惊恐", "大笑"]));
   e1x.data = j1x.cr2x({
       params: bVC0x.encText,
       encSecKey: bVC0x.encSecKey
   })
   ```

   通过测试，发现就是这一个地方，删除其他断点。

   发现这一个地方再点击下一页的时候，会被断三次？

   断点这个地方被调用了多次【函数的多次调用，多次请求】 =》确定哪一次是翻页那一次的。

   ​	查找调试中有没有与需要发送请求那个相关的信息【参数密文、参数明文、url、关键字】。

   注意事项：当前网站有缓存，点击过的页面，需要刷新之后再点击才会进入翻页断点部分。

#### 扣代码

参数：

尽量将代码简化，能固定的固定，不能固定的去找。

```js
var bVC0x = window.asrsea(JSON.stringify(i1x), bss6m(["流泪", "强"]), bss6m(Af6Z.md), bss6m(["爱心", "女孩", "惊恐", "大笑"]));
e1x.data = j1x.cr2x({
    params: bVC0x.encText,
    encSecKey: bVC0x.encSecKey
})
```

修改：

```js
var i1x = {
    "rid": "R_SO_4_204072",
    "threadId": "R_SO_4_204072",
    "pageNo": "3",
    "pageSize": "20",
    "cursor": "1566375102350",
    "offset": "0",
    "orderType": "1",
    "csrf_token": "264449941cdd556f54939af1d085a1f2"
};

var bVC0x = window.asrsea(
    // i1x => 翻页信息
    JSON.stringify(i1x)
    // 函数（固定的数组） =》
    // , bss6m(["流泪", "强"])
    , '010001'
    // , bss6m(Af6Z.md)
    , ''
    // , bss6m(["爱心", "女孩", "惊恐", "大笑"])
    , '0CoJUm6Qyw8W8jud'
);

function get_params(){
    return {
        params: bVC0x.encText,
        encSecKey: bVC0x.encSecKey
    }
}

console.log(get_params());
```

函数：

- 找 window.asrsea 函数

- d 函数

  ```js
  function d(d, e, f, g) {
      var h = {}
        , i = a(16);
      return h.encText = b(d, g),
      h.encText = b(h.encText, i),
      h.encSecKey = c(i, e, f),
      h
  }
  ```

- 将代码格式修改

  ```js
  var bVC0x = d(
      JSON.stringify(i1x)
      , '010001'
      , ''
      , '0CoJUm6Qyw8W8jud'
  );
  ```

执行程序之后，根据提示缺啥补啥：

1. ReferenceError: a is not defined

   找到 a 所在的位置，发现是一个函数，根据找函数的方式去找。

   找到的代码尽量按照位置存放。如果不知道，尽量放到调用的上面。

2. ReferenceError: CryptoJS is not defined

   看代码发现是以个对象。

   变量的定义方式：

   - var
   - ，

3. TypeError: Cannot read properties of undefined (reading 'encrypt')

   谁没有定义？

   在错误位置打断点，在控制台打印分析谁没定义。

   CryptoJS.AES 没定义。

4. ReferenceError: setMaxDigits is not defined

   往后，是大批量的函数扣取：

   前面是从 CryptoJS 后开始：

   ![image-20251219215200177](D:/tool/%E7%88%AC%E8%99%AB2/2511%E6%9C%9F2%E7%8F%AD%E7%88%AC%E8%99%AB-harry%E8%80%81%E5%B8%88/13.%E6%89%A3%E5%8F%96js%E4%BB%A3%E7%A0%81/02.%E4%BB%A3%E7%A0%81/assets/image-20251219215200177.png)

5. ReferenceError: bitsPerDigit is not defined

   变量

6. TypeError: Cannot read properties of undefined (reading '0')



注意事项：

- 将函数在控制台打印：

  ```js
  ƒ stringify() { [native code] }
  ```

  代表是本地【内置】函数，不需要扣。

- 找参数、函数，一定是要将程序执行到对应的位置之后再查看。

- 并不是逆向得到了密文就表示成功【将密文给 python 去发送请求，并且能正确得到结果才是成功。】



快速定位到代码行数：

- ctrl+g：输入函数，回车





### 注：

cursor值

```
	"rid": "R_SO_4_204072",
    "threadId": "R_SO_4_204072",
    "pageNo": "4",
    "pageSize": "20",
    "cursor": "1531897295401",
    "offset": "0",
    "orderType": "1",
    "csrf_token": "264449941cdd556f54939af1d085a1f2"

```

它会随翻页而变化，可知它能在 **网络** 的 **响应** 中找到





## 艺恩  

### 需求:响应文本解密定位

网址：[艺恩-数据智能服务商_视频播映指数](https://www.endata.com.cn/Video/index.html)

接口：https://www.endata.com.cn/API/GetData.ashx



1.根据 **网络** 定位请求

2.在 **发起程序** 中找到调用堆栈

3.根据堆栈特性找到发送请求的函数（即最上方的）

4.分析找到密文，进而找到解密函数







## 极志愿【异步】

### 需求

网址：[学院-院校库专业库-极志愿](https://www.jizhy.com/42/search?searchVal=学院)

接口：https://www.jizhy.com/searchcenter/search

需求：参数逆向定位【sign】

### promise.then

官网：[Promise - JavaScript | MDN](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise) 

异步。

- **Promise.reject()** 静态方法返回一个已拒绝（rejected）的 `Promise` 对象，拒绝原因为给定的参数。

- **Promise.resolve()** 静态方法以给定值“解决（resolve）”一个 [`Promise`](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise)。如果该值本身就是一个 Promise，那么该 Promise 将被返回；如果该值是一个 [thenable](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise#thenable) 对象，`Promise.resolve()` 将调用其 `then()` 方法及其两个回调函数；否则，返回的 Promise 将会以该值兑现。

  该函数将嵌套的类 Promise 对象（例如，一个将被兑现为另一个 Promise 对象的 Promise 对象）展平，转化为单个 Promise 对象，其兑现值为一个非 thenable 值。

- then：[`Promise`](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise) 实例的 **`then()`** 方法最多接受两个参数：用于 `Promise` 兑现和拒绝情况的回调函数。它立即返回一个等效的 [`Promise`](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise) 对象，允许你链接到其他 Promise 方法，从而实现[链式调用](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Using_promises#链式调用)。

  then 的第一个参数是成功回调函数，第二个是失败回调函数。



注：

所以要注意then



### 定位

1. 确定加密位置【明文密文】

   - 上：上面的最下面那个都是密文，所以是在更下面实现的加密

   - 下：打断点，然后一个个栈查看

   - 中：在下的最上面个栈打断点，执行过去

     n = n.then(e.shift(), e.shift());

2. n = n.then(e.shift(), e.shift());

   n.then 需要两个参数【回调函数】

   e.shift() 是从数组里面弹出一个元素。

   在控制台打印数组，对所有的函数打断点，继续执行。

3. 可疑 1 

   ```js
   onRequest: function(t) {
       this.interceptors.request.use((function(e) {
           return t(e) || e
       }
                                     ))
   },
   ```

   这里的 t(e) 有可能是加密？？？

4. 进入 t 函数

   ```js
   e.onRequest((function(t) {
       var e = t.method;
       "get" === e ? t.params = Qt(t.params) : "post" === e && (t.data = Qt(t.data))
   }
   ```

   Qt(t.params) 部分执行之后就能得到加密的密文，确定加密位置。

















